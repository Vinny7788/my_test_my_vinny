using System;

namespace School
{
    internal class Program
    {
        static void Main(string[] args)
        {
            Rander map = new Rander();
            Player player = new Player(10, 15, '$');

            Console.CursorVisible = false;

            map.DrawPlayer(player);

            Console.ReadKey(true);
        }
    }

    class Rander
    {
        public void DrawPlayer(Player player)
        {
            Console.SetCursorPosition(player.PositionX, player.PositionY);
            Console.WriteLine(player.Symbol);
        }
    }

    class Player
    {
        public Player(int setPositionX, int setPositionY, char symbolSet)
        {
            PositionX = setPositionX;
            PositionY = setPositionY;
            Symbol = symbolSet;
        }

        public int PositionX { get; private set; }
        public int PositionY { get; private set; }
        public char Symbol { get; private set; }
    }
}

