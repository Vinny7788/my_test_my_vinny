using Microsoft.Win32;
using System;

namespace School
{
    internal class Program
    {
        static void Main(string[] args)
        {
            int[] arrayForReset = new int[0]; 
            int[] mainNumbersArray = new int[1];
            int sumOfNumbers = 0;
            string exitWord = "exit";
            string summaWord = "sum";
            string userInput;
            bool isWork = true;

            Console.WriteLine($"You can write the number or command: /{exitWord}/ to exit the program , /{summaWord}/ to calculate the entered numbers.  ");
            while (isWork)
            {

                Console.WriteLine("Write the number or command:");
                userInput = Console.ReadLine();
                Console.Clear();

                if (userInput.ToLower() == exitWord)
                {
                    isWork = false;
                }

                else if (userInput.ToLower() == summaWord)
                {

                    for (int i = 0; i < mainNumbersArray.Length; i++)
                    {
                        sumOfNumbers += mainNumbersArray[i];
                    }

                    Console.WriteLine($"Summa is : {sumOfNumbers}");
                    sumOfNumbers = 0;

                    for(int i = 0;i < mainNumbersArray.Length;i++)
                    {
                        mainNumbersArray[i] = 0;
                    }

                }
                else
                {

                    int[] tempNumbersArray = new int[mainNumbersArray.Length + 1];

                    for (int i = 0; i < mainNumbersArray.Length; i++)
                    {
                        tempNumbersArray[i] = mainNumbersArray[i];
                    }
                    tempNumbersArray[tempNumbersArray.Length - 1] += Convert.ToInt32(userInput);
                    mainNumbersArray = tempNumbersArray;
                }
            }
            Console.Clear();
            Console.WriteLine("Goodbye!!!");
        }
    }
}
